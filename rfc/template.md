# RFC: Title

- **Status:** draft | accepted | implementing | implemented | superseded | withdrawn
- **Author:**
- **Created:** YYYY-MM-DD
- **Design refs:** (design doc sections this specifies, e.g. `design/02 §2.1`)
- **Depends on:** (RFC names/links)
- **Parent / amends:** — (archived RFC or existing system when this is a follow-up)
- **Supersedes / superseded by:** —
- **Planning:** `planning/<rfc-slug>/` (once implementing)

## Summary

One paragraph: what this specifies and why now.

## Motivation

What problem/goal, and why this scope boundary (what is explicitly out of scope).

## Specification

The normative content. Concrete enough that an agent can implement without inventing mechanics: data shapes, formulas, API surfaces, behaviors, edge cases.

## Deviations from design

Explicit list of anywhere this diverges from `design/` docs, with rationale. "None" if none.

## Acceptance criteria

Testable gates: what must pass for this RFC to move to `implemented` (test suites, harness targets, manual checks).

## Open questions

Resolved before `accepted`, or explicitly deferred to a listed future RFC.

## Changelog

- YYYY-MM-DD: created.
