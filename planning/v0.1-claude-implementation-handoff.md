# v0.1 implementation wave — designated-review handoff (Claude → Codex)

**Status:** review input for **Codex**, the cross-party designated reviewer for Claude-authored
implementation under AGENTS.md (c). This is not a verdict. It makes no archival, mint or release
claim. **Prepared:** 2026-09-25.

On 2026-09-24/25 the owner directed Claude to implement, and accepted 12 v0.1 RFC drafts in one
batch (`84a99b58`). Claude, together with its delegated sub-agents, implemented every accepted RFC
not blocked on an owner ruling. Each range below carries its own predeclaration, evidence and
severing record in the per-RFC `planning/<slug>/log.md`. Every entry reads "Implemented by: Claude;
awaiting Codex designated review", and none is self-approved.

## Ranges to review

| RFC / lane | Range (inclusive) | Planning log |
|---|---|---|
| Minigame API & Surface: Pitch surface, recovery surface, composed witness, UI-path move | `50a3a514`, `06edddba`, `b92a05de`, `ba185f13`, `79ff1aa1` | `planning/minigame-api-and-surface/log.md` |
| AR-F1 zero-credit resolution, plus the kernel correction | `8add475`, `0cf9f7a` | `planning/minigame-platform-foundation/log.md` |
| F9 Wind Down preview with an active session | `2def3612` | `planning/minigame-api-and-surface/log.md` |
| API Foundation: epochs reader, query descriptors | `cd5ba06d..e63796d5` | `planning/api-foundation/log.md` |
| API Foundation: public router, boards, routes, AC5 privacy | `5825ad24..09d12987` | `planning/api-foundation/log.md` |
| Terminal Typer | `8d5649ba..10a1e965`, `97694dfe` | `planning/minigame-terminal-typer/log.md` |
| Garage Player Surfaces, batch A | `6458bc02..c097b5d2` | `planning/garage-player-surfaces/log.md` |
| Garage Player Surfaces, continuation (GS4/GS5/decoders/reflow) | `d035b8f8..1be94b0c` | same |
| Reputation Tree v1 | `08412bf8..ec47af68` | `planning/reputation-tree-v1/log.md` |
| Fast-harness regression (Reputation evidence → maintenance) | `68777ab8` | same |
| Pet Adoption v1 | `e5b7541a..f90334bf` | `planning/pet-adoption-v1/log.md` |
| Tier 2 content, fixture-first, no seats | `762bc0e9..90a54f55`, `a63740cd` | `planning/tier2-content/log.md` |
| Cosmetic Shop v1 | `8d88248a..c20d23f1` | `planning/cosmetic-shop-v1/log.md` |
| Clout v1 + PR Interns | `4c74ec74..eda4b0b4`, `2f2c5a04`, `527246f1` | `planning/clout-v1-and-pr-interns/log.md` |
| Demo Disc Arcade, below the public wire | `a6d95d32..6dbbc0df` | `planning/minigame-demo-disc-arcade/log.md` |
| Server Garden | `9f61fa45..725d8662` | `planning/minigame-server-garden/log.md` |
| Designated review Claude recorded on Codex's D-014 CI range | `f13b808` | `planning/platform-alignment/log.md` |
| RFC drafts, acceptance record, Pinball draft (RFC text: needs Codex's RFC review) | `17a1f3b`, `6a86f44`, `7e72036`, `c11ed52`, `2c4f6a5`, `9072552`, `b7c14e0`, `84a99b58`, `f98c71cb` | `rfc/*.md` |

Ranges written `a..b` are inclusive of `a`. Commits by other lanes may be interleaved inside a
range; the owning log names each lane's exact commit set.

## Self-disclosed process issues to examine

1. **Kernel guard history.** `50a3a514`, `b92a05de` and `79ff1aa1` changed kernel-guarded paths
   (`client/src/minigame/`, `client/src/soul/`) with presentation-only files and no version bump.
   `8add475` changed real semantics without a same-commit bump; its append-only correction is in
   `0cf9f7a` and needs a `## ` review section citing (`8add475^..8add475`).
   `make verify-kernel-version` is therefore red at HEAD, on these items only. Claude's attempt at
   the CLAUDE.md-permitted rewrite was denied by the session permission classifier. Whether to
   rewrite or to add corrections is an **owner decision** that is still open.
2. **Optional-arm pattern under C2.** Several lanes added snapshot data as *optional* v4 fields,
   not the required fields their RFCs name, because the compatibility gate rejects required
   additions: `features.pet_adoption`, `features.opportunity`, `features.cosmetics`,
   `features.reputation`, `axis_stack`. Garage batch A re-pinned for v4 on GS0.1 authority, and
   that re-pin needs confirmation. No other lane re-pinned.
3. **The Typer/Arcade public wire is intentionally unbuilt.** TT-PA4/AR-P arms conflict with C2
   (owner ruling pending).
4. **Numbering by landing order.** RFC texts claiming Founder v22, snapshot v4 and similar were
   mapped to the next free number: Reputation v22, Pet v23, Cosmetics v24, Garden v25; Company
   v19 is Clout; replay inputs run to v12.
5. **Shared Postgres interference.** Some combined runs failed while another lane's container was
   using the same database. Each is logged, with a clean single-package rerun, and none is counted
   as a pass.
6. **Candidate copy everywhere.** All new player text is candidate or `PENDING OWNER COPY` in
   `copy/catalog/*-candidate.json`. None of it is ruled copy.

## Open owner decisions blocking further work

- Reputation threshold (1e5 recommended) and the starter strength for the Casual persona (H4).
- Typer/Arcade API route (new game-specific v1 operations recommended).
- Tier 2 seat source (generator-granted recommended), gate literal/envelope, and relevance
  (headcount first, then retune).
- PR Intern harness attainment (a harness-only observer recommended, DG-D).
- Pinball acceptance.
- Kernel-guard history (rewrite vs corrections).
- The owner-gated v0.1 content mint.
