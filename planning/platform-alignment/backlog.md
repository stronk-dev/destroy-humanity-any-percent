# Tracked alignment backlog

This is the tracked interim ledger for repository/release findings while `design/BACKLOG.md` and
`design/research/*` remain intentionally gitignored from the public repository. It does not replace
the internal design backlog. D-002 must decide the durable publication/storage model; until then,
every alignment finding is recorded in both places so a fresh clone does not lose the program.

| ID | Observation | Class | Route |
|---|---|---|---|
| RP-001 | Current-head hosted CI never reaches a verdict; the harness is killed at 30 minutes in five consecutive current-product runs. | defect | R-001 / CI Baseline |
| RP-002 | Production Docker/Caddy/Compose packaging is absent. | release defect | D-001/D-006 / Deployment |
| RP-003 | Backup, restore, rollback, and disaster rehearsal are absent. | release defect | R-006 |
| RP-004 | Player data export has no endpoint, schema, UI, or proof. | release defect | D-008 |
| RP-005 | Account deletion is backend-only and has no player workflow/disclosure. | integration defect | D-009/R-003 |
| RP-006 | One-time recovery credentials are silently stored with no display/download/recovery workflow. | risk/integration defect | D-005/R-003 |
| RP-007 | The ruled local-only outage fallback and import path are absent from the production client; older tech/Account text also overstates offline mode relative to the adopted server-anonymous default. | intent/body/implementation contradiction | ruling-author reconciliation plus account successor |
| RP-008 | Game UI stops at bootstrap-to-Desk: accepted snapshot-v3 transition eligibility, Gate/Wind Down controls, and Run End→next-run continuation are absent; AC1 still demands the C28-rejected full browser replay. | implementation plus owner-ruling/body defect | ruling-author C25–C28 reconciliation, then narrowed Game UI AC1 / R-004 |
| RP-009 | Accessibility proof does not cover complete assistive user workflows. | release proof gap | R-005 |
| RP-010 | Only T0–T1 and one minigame exist; most of the designed product and ending are absent. | scope question | D-001/D-007 |
| RP-011 | Client third-party license delivery is absent. | packaging defect | Deployment |
| RP-012 | Sunset/self-host/export promises are unimplemented intent. | owner question | D-003/R-006 |
| RP-013 | Repository disposition contradicts itself: public GitHub; internal records say private/local-only. | owner question | D-002 |
| RP-014 | Current state, README, Deployment RFC, RFC handoff, coverage map, and live plans disagree with HEAD. | record defect | lifecycle reconciliation |
| RP-015 | The mandated shared `design/BACKLOG.md`, research matrix, dossiers, and coverage map are gitignored, so a fresh clone cannot receive the supposed shared memory. | process defect | D-002/D-010; tracked interim ledger |
| RP-016 | Minigame Platform says its normative body is reconciled and requires a combat-duel tenant, while its later ruling, combat RFCs, catalog tree, and production registry prove no such tenant exists. | normative contradiction | ruling author reconciliation before Minigame Platform acceptance/archival |
| RP-017 | `design/03` rules that paying hobbies never restore Soul and names missing §5c as the sole recovery source, but later calls Go restorative and explicitly grants Arcade Soul restoration. | owner-authored design contradiction | design author reconciliation; no RFC inference |
| RP-018 | `design/11`'s cold-open specimen displays a fabricated WR while its later owner ruling forbids any WR before validated boards exist. | owner-authored design contradiction | design author reconciliation before shipping cold-open copy |
| RP-019 | `design/12` specifies a `MatchActor`/tick minigame socket, while the accepted platform contract and runtime use pure tenants plus DB-authoritative sessions. | design/RFC architecture drift | design deviation reconciliation; Minigame Platform body repair |
| RP-020 | `design/12` still says production ships by “pack push + hot reload” immediately before a ruling that production hot reload is forbidden and epoch-stamped deploys are mandatory. | owner-ruling/body contradiction | ruling author reconciliation |
| RP-021 | The T0-T1 capacity change claimed a measured hosted harness lane, but no Actions run exists for that commit; archival consumed native/Docker checks, and every later hosted run exceeds the adopted ceiling. | acceptance/closeout defect | R-001 / CI Baseline successor |
| RP-022 | `HARNESS_WORKERS` parallelizes only standard pacing; relevance registry rows and their internal experiment arms remain serial. | performance/architecture defect | R-001 measurement, then CI Baseline amendment |
| RP-023 | `mode=check` emits no phase/row progress or termination artifact, so a hosted kill discloses neither completed population nor invalidation state. | evidence/instrumentation defect | R-001 / fail-loud diagnostic contract |
| RP-024 | Account AC2 has real family-revocation tests and transport reauthentication code, but no fixture composes revocation with an already-connected socket and proves the required subscribe/disconnect boundary. | acceptance proof gap | Account/Transport integrated negative witness |
| RP-025 | Root Make test selectors expand `SAVE_TEST_FLAGS` unquoted; a valid parenthesized Go `-run` regex becomes shell syntax and the declared focused lane never starts. | tooling defect | CI Baseline selector hardening with a seeded regex fixture |
| RP-026 | Game UI AC4's browser fixture asserts only accessibility/mechanical-ID absence; suppressing cap explanation, drain notice, and resync story beat together leaves all 20,007 browser assertions green. | acceptance-oracle defect | three independent Game UI AC4 behavior fixtures with one-seam failing mutations |
| RP-027 | Permits P2, P4, and AC4 still say “T3-era,” invoke a nonexistent generator-copy convention, and require a separate pre-FCE `BALANCE-CHANGE:` despite PT-C1/PT-C2/PT-C4 ruling the opposite. | owner-ruling/body contradiction | ruling author reconciliation before Permits closeout |
| RP-028 | Permits AC3 and its plan require a Go/TypeScript replay row for the exact Cash+Permits T3→T4 crossing, but the shared T3→T4 replay fixture is cash-only and the First Content Exit fixture is a Tier-1 wind-down. | acceptance proof gap | accepted Permits/FCE replay fixture plus designated review |
| RP-029 | Canonical docs still say the active economy is pre-mint/schema v3 and production has no T3→T4 gate; other fixture-first docs also retain “not pinned until FCE” text after epochs 6–8 minted. | canonical-doc defect | transactional Permits/FCE docs reconciliation |
| RP-030 | First Content AC4 requires the epoch-6 changelog to cite every consumed verdict and reviewed range, but several artifact rows name only a generic archived verdict/log or a verdict commit without its exact range. | closeout/provenance defect | exact changelog coordinates plus review |
| RP-031 | First Content FCE5.5 and AC5 still require green “at the mint commit,” although the accepted post-mint ruling changed the contract to range-head green; the author appended a note without reconciling the normative body. | owner-ruling/body contradiction | ruling author reconciliation before FCE closeout |
| RP-032 | The FCE activation tests pinned exact epoch 6 at the mint, but later epoch commits changed their helper to deploy-current epoch 8; HEAD no longer has an epoch-6-specific activation regression witness. | historical acceptance regression | fixed epoch-6 bundle activation fixture, retaining deploy-current coverage |
| RP-033 | Epoch-7 content-dynamics contracts were appended to the bounded, already-minted FCE RFC and recorded in a separate live plan while their implementation ultimately shipped through archived T0–T1; the active FCE lifecycle has no coherent remaining scope. | RFC scope/lifecycle defect | split/reconcile successor authority, then transactional FCE closeout |
| RP-034 | Prestige D2 uses stale payload names, D3/P4 retain revision-archive semantics contradicted by P4b, and D4/P5 plus canonical docs retain the superseded threshold-crossing scripted trigger instead of the shipped first-command curriculum. | owner-ruling/body and canonical-doc contradiction | ruling author/body reconciliation plus transactional docs correction |
| RP-035 | Advisor Mode has persisted state, multiplier math, replay carry, and Assisted event labeling, but no accepted toggle intent, settings control, first-enable transition, or player copy surface. | long-open design/integration gap | owner rules Prestige D5 intent/successor; no implementation inference |
| RP-036 | Prestige P3 still declares Quarter harvest as a live offer-spawn site while an inline note defers the missing Founder-to-Company consumer; appending the deferral without rewriting the normative sentence violates body reconciliation. | owner-ruling/body and implementation contradiction | ruling author reconciles P3 and routes the bridge to a successor |
| RP-037 | Prestige AC2–AC6 overstate their exact witnesses: no offer-age property, full-domain reseed/non-empty repeated ledger proof, integrated New-Founder lifecycle, eligible-state/mid-chain matrix, or checked-in run-2 golden exists. | acceptance proof gap | accepted witness-only remediation with failing cases and designated review |
| RP-038 | Prestige has historical adversarial-review prose, but no tracked verdict with explicit cross-party provenance and post-rewrite ranges unioning the full implementation/remediation span; the only final-remediation review text is in a gitignored untracked file. | archival review/provenance gap | new tracked designated cross-party full-range verdict before archival |
| RP-039 | Leaderboards AC5 is false at HEAD: `run_ended.assisted.commons` records Compact membership only at Exit, and the projector never scans Compact lifecycle events, so join→leave→Exit is ranked Solo despite the accepted “membership at any point” rule. | implementation and acceptance defect | accepted Leaderboards remediation with discriminating lifecycle/replay/projection fixtures |
| RP-040 | Exact board storage/projection exists, but production exposes no board/epoch HTTP reader and the client has no leaderboard, historical browse, validator, or combined Route Registry/records surface. | producer-without-consumer integration defect | accepted public reader + player-surface successor; do not call backend foundation shipped capability |
| RP-041 | Leaderboards AC1, AC3, and AC6 lack their literal witnesses: identical magnitude keys stand in for distinct sub-quantum values, cross-epoch pin/Exit is not composed with replay+ranking in the old epoch, and no original board is queried after two later mints. | acceptance proof gap | witness-only remediation with demonstrated failing cases and designated review |
| RP-042 | Leaderboards D2/D4/D5/D6/L1 and canonical implementation truth disagree over verdict count, category count, deferred player/Exhibition surfaces, mandate objects, world-first dispatch, machine boards, and abandoned-run retention. | owner-ruling/body and scope contradiction | ruling-author reconciliation plus explicitly accepted successors |
| RP-043 | The Leaderboards plan predates the archived Run Genesis successor, while its early review entries lack current explicit provenance/post-rewrite range union; the completed Run Genesis remediation thread also remains in the live planning namespace. | lifecycle and archival review defect | transactional plan/thread reconciliation followed by a tracked cross-party full-range verdict |
| RP-044 | Minigame offline-quality decay is behaviorally dead: result resolution computes decay then overwrites it with the new result, start reads the stored grade unchanged, and no automated-output consumer applies the declared destination. | implementation and acceptance defect | accepted Minigame Platform AC2 remediation with no-play decay/application and mutation witnesses |
| RP-045 | Minigame Platform AC1 and AC3 overstate integration: production hardcodes solo, async-snapshot has no composed tenant lifecycle, and bot reduction is proven only by schema/pure arithmetic without a bot match. | acceptance/integration gap | owner scope ruling for async; bot witness remains dependency-blocked on accepted Combat children |
| RP-046 | Minigame Platform's RFC, plan, canonical docs, and archived Pitch dependency disagree about schema, production activation, composition, decay, and whether the platform is active or archived. | normative/canonical/lifecycle contradiction | ruling-author body repair plus transactional plan/docs/successor reconciliation |
| RP-047 | Minigame foundation, Pitch, FCE, and API have substantial separate designated verdicts, but the active platform record has no final criterion trace or exact cross-RFC range union covering the implementation it would consume for archival. | archival review/provenance gap | construct exact post-rewrite union and obtain tracked cross-party closeout verdict |
| RP-048 | The production Game UI stores a 15-minute access token and 30-day refresh token but never rotates credentials or reauthenticates the socket; expiry collapses into a generic offline state with no recovery action. | session lifecycle/integration defect | accepted Account/API/Game-UI successor with expiry→rotation→retry→reconnect proof |
| RP-049 | Account AC3/AC5/AC6/AC7 lack their literal discriminators: repeated free New Founder, actual import→board exclusion, retention of the active imported stream on deletion, and rate limits on all three unauthenticated routes. | acceptance proof gap | accepted witness-only remediation with failing mutations and designated review |
| RP-050 | Account's plan/log end before bootstrap, composition, credential GC, API, transport, and UI successors; review entries cite non-resolving pre-rewrite hashes and lack current explicit provenance/range union. | lifecycle and archival review defect | transactional plan/docs reconciliation plus exact current-history cross-party verdict |
| RP-051 | Anonymous account creation is only bounded by resettable in-memory IP buckets; there is no abandoned-account/save retention quota or reaper, an open storage-amplification finding since the first adversarial review. | operational abuse/retention defect | owner retention ruling and accepted deployment/account cleanup contract |
| RP-052 | The production Game UI never stores Centrifuge epoch/offset, requests recovery, reconnects/resubscribes, interprets typed close codes, or honors `resume_after_ms`; Transport AC2/AC4/AC5 therefore stop at server-side protocol drivers. | producer-without-consumer integration defect | accepted Transport/Game-UI recovery controller with real disconnect/overflow/drain/expiry proofs |
| RP-053 | `PlayerRevisionCursor` is imported only by its unit test; live player events bypass per-scope gap detection, duplicate suppression, snapshot reset, and historical-compensation cursor semantics despite RFC/canonical claims. | orphaned safety primitive and canonical-truth defect | bind the cursor at the production runtime boundary with duplicate/gap/historical negative fixtures |
| RP-054 | Transport AC3 and AC6 lack their literal discriminators: the stalled-world test runs for under one second and permits two frames, and no test rejects a non-member Guild subscription. | acceptance proof gap | exact 10-second/exact-one ruling or body reconciliation plus real non-member Guild negative |
| RP-055 | Transport plan item 4 is obsolete, T6 still claims pre-Guild composition, the local log ends with review pending, and pre-rewrite review hashes are not reconciled into a current full-span union including later soak/composition/browser work. | normative/lifecycle and archival-review defect | author/plan reconciliation, rewrite-map translation, then tracked cross-party full-range verdict |
| RP-056 | CI's normative four-job/sub-five-minute/body-excludes-harness contract and “first hosted run pending” plan contradict the six-job, 30-minute blocking topology and repeated hosted cancellations. | owner-ruling/body and lifecycle contradiction | ruling-author topology/latency reconciliation plus transactional plan/docs repair |
| RP-057 | CI AC5 is false in two ways: harness/numeric setup-go defaults cache build outputs despite dependency-only claims, and scheduled/manual events run all blocking jobs while numeric failures fail the supposedly non-blocking nightly workflow. | workflow implementation/canonical contradiction | adopted cache/nightly semantics plus discriminating workflow-structure tests |
| RP-058 | CI review evidence is fragmented across active and successor logs; no tracked exact current-history range union covers the initial baseline and every workflow/Make/schema repair for archival. | archival review/provenance defect | enumerate mapped ranges and obtain one tracked cross-party closeout verdict |
| RP-059 | R-001 requires harness observability code, but the active CI RFC explicitly excludes the balance harness and the relevant harness/content RFCs are archived. | implementation-authority gap | accepted CI amendment or harness-observability RFC before instrumentation |
| RP-060 | API Foundation's “one operation authority” covers only 10 of 21 live `/api/v1` routes; changing an unregistered Founder response leaves `make api-check` green. | contract/implementation and acceptance defect | migrate all legacy routes under accepted A5/C1 authority with handler-level drift negatives |
| RP-061 | The public API is entirely uncomposed: zero public generated paths, test-only policy/cursor runtime, no readers/evidence, and no historical formula artifact in the current 19-artifact epoch set. | producer-without-consumer and sequencing defect | accepted API public-reader/composition scope plus protocol-compliant formula mint |
| RP-062 | API compatibility rejects a removed nested field but names only the root schema, while 11 live routes have no compatibility pins; AC2's literal field-named/full-v1 proof is false. | acceptance-oracle and coverage defect | exact property-path diagnostics, complete route pins, and restored command-level mutations |
| RP-063 | API AC4 still contradicts its C9 ruling; production makes raw injected-fetch API calls and the boundary lint ignores non-Svelte Game UI runtime files, accepting a direct `/api/v1` fetch probe. | owner-ruling/body and implementation contradiction | ruling-author body reconciliation, one generated/thin client, all-source negative lint |
| RP-064 | The operation/generator authority cannot express public board query parameters, typed raw-success clients, or ruled request-ID/cache/ETag/deprecation response headers; generated OpenAPI has none of those headers. | accepted-contract implementation gap | extend A4–A8/C13/C19 registry/generator before registering public operations |
| RP-065 | API plan/log/docs describe an older authenticated-only slice, C18 sequencing is stale after epochs 6–8, and no exact current-history cross-party range union covers the complete foundation. | normative/canonical/lifecycle and archival-review defect | author/record reconciliation followed by tracked full-span designated verdict |
| RP-066 | Game UI AC5 consumes neither its 4× CPU-throttle nor dropped-frame budget, runs 1,200 “60-second” inputs in a subsecond fixture, and has no recorded manual reference-device result; the log overclaims it as fully met. | acceptance measurement/canonical qualification defect | retain CI count/long-task arm; add fail-loud instrumentation and ruled manual throttle/frame record or author reconciliation |
| RP-067 | Game UI's header/U2/AC1 and canonical doc omit or contradict GU-C7/C25–C28; successor verdicts are not assembled into one current full-range union and the plan's body-reconciled checkbox is false. | owner-ruling/body/canonical/lifecycle and archival-review defect | ruling-author body repair, docs/plan reconciliation, exact current-history designated closeout |
| RP-068 | Minigame API AC2's global `APIError` schema admits invalid category/detail cross-products, and its “byte-match” table uses substring containment; appending response bytes leaves the Account suite green. | wire-authority and acceptance-oracle defect | operation/status literal error-pair authority plus exact-byte negatives for every deterministic mapping |
| RP-069 | Minigame API AC3 proves command-flood nonmutation, but Recovery flooding is a 429 against a stateless adapter with no before/after progress, token, or session comparison. | acceptance proof gap | composed/stateful heartbeat flood → unchanged authoritative state → refill/resume witness |
| RP-070 | Minigame API AC4's privacy enumeration covers only four minigame operations; adding client `founder_id` to a Recovery finish schema leaves every Account unit test green. | acceptance-oracle and coverage defect | enumerate all eight registered RFC operations, server-owned request fields, response hidden-info, and per-family mutations |
| RP-071 | Minigame API Summary/MA1/MA2/MA3/header/plan contradict C1/C3/C5/C8/C9 and current dependencies; AC5 has no exact buildable surface contract, generated HTTP client, components, or assembled current review union. | owner-ruling/body, producer-without-consumer, lifecycle and archival-review defect | ruling-author reconciliation/exact surface contract, API-client dependency, both visible workflows, then current full-span cross-party verdict |
| RP-072 | Combat C2 never enumerates the promised closed move-effect/spell schemas, and C5 provides no literal Trust/Obedience/Soul table points, domains, interpolation, clamp, or composition order; AC1/AC5 cannot be implemented honestly. | owner decision/design specification gap | ruling-author exact catalog/effect/table contract before parent implementation resumes |
| RP-073 | Combat's plan/ledger understate implemented AC4/client-AC6 work, while every review cites non-resolving pre-rewrite hashes without explicit reviewer/recorder/ranges and no final pass covers round-three repairs. | lifecycle and archival-review defect | plan/ledger reconciliation, hash mapping or superseding current-span review, designated cross-party verdict |
| RP-074 | Combat C3 bans native `/` “anywhere in combat paths” and AC6 names `combat/`, but the gate scans only client modules while Go `floorRatio` uses native division; the client half discriminates and canonical docs quietly narrow the claim. | normative/implementation and acceptance-scope contradiction | ruling-author runtime-scope reconciliation or Go boundary/helper plus matching docs and negative probe |
| RP-075 | Production cannot consume the declared deployment origin/proxy policy: WebSocket origin is catalog-pinned to localhost and Account trusted-proxy depth is hardcoded to zero. | deployment configuration defect | D-006 / reconciled Deployment DP5 |
| RP-076 | Account key-rotation primitives are not composed: gameserver accepts one JWT key with hardcoded ID and one bootstrap key; previous and cursor keys are not runtime-configurable. | deployment/security integration defect | D-006 / reconciled Deployment DP5 |
| RP-077 | The built gameserver is repository-disk-dependent, embeds no catalogs/client, and serves no static assets despite the draft's single-artifact claim. | packaging/normative contradiction | reconciled Deployment DP1 / RP-002 |
| RP-078 | Deployment AC5's public push is already true while the draft/index still route “THE PUSH” as future work; the remaining acceptance rows are not implemented. | lifecycle/normative contradiction | Deployment author reconciliation before acceptance |
| RP-079 | Missing, malformed, or empty-string browser credentials have no recovery branch: they silently create a replacement account or strand the UI offline, orphaning the prior account from the player. | account recovery/failure-state defect | D-005/R-003 and accepted Account/Game-UI successor |
| RP-080 | Settings says offline progress is parked locally, but the client has no local gameplay save, durable intent queue, import owner, or reconnect/flush path. | player-copy/implementation contradiction | ruling-author copy reconciliation plus accepted offline-fallback successor |
| RP-081 | `settingsConfirmation()` is test-only and no destructive Settings control exists; the apparent navigation safety primitive has no deletion/export/New-Founder consumer. | orphaned safety primitive/integration defect | exact Account Settings surface contract under D-008/D-009 |
| RP-082 | Authoritative Offer/Run-End surface replacement removes the focused Desk control and leaves focus on `<body>` with no heading focus or surface announcement; the defect reproduces in Chromium, Firefox, and WebKit. | accessibility implementation/oracle defect | accepted cross-surface accessibility contract / R-005 |
| RP-083 | The complete Desk is 647 px wide in a 320 px container in all three browser engines, while the declared browser suite uses only 1280×720 and stays green. | WCAG reflow/responsive-layout defect | accepted accessibility/UI layout contract with 320 px and zoom witnesses |
| RP-084 | Production reduced motion zeroes sampled CSS tokens only: GameUIShell leaves numeric interpolation/pulse at `reducedMotion=false` and no listener handles mid-session preference changes. | accessibility integration defect | accepted shell/theme preference owner with live-change negative |
| RP-085 | Production exposes liveness/readiness and fatal-job propagation but no metrics exporter/route, request/access correlation, alerts, SLOs, dashboard, or operator runbook; Production invariant metrics are composed as nil. | observability and operations integration defect | D-006 plus accepted operations/deployment contract with fired alert/counter negatives |
| RP-086 | `Store.PruneIntentRecords` is package-tested but has no production caller, contradicting canonical Save documentation's 30-day deployment-scheduler policy. | retention implementation/canonical-truth defect | retention ruling plus composed bounded cleanup job and docs reconciliation |
| RP-087 | Anonymous accounts, archived Founders/streams, append-only events/logs, dead letters, and projection/moderation history have no complete accepted retention schedule or inactivity cleanup/disclosure. | privacy, storage-abuse, and owner-policy gap | D-009 plus complete data inventory/legal review and accepted retention RFC |
| RP-088 | Designed-sunset research says a one-command self-host deliverable already exists and the covenant is not an engineering project, but only test Compose files and a disk-dependent server binary exist; client/export/bots-default/artifact/mirror/runbook are absent. | research-current-state contradiction and preservation gap | reverify/reconcile research, D-003, Deployment/R-006, then sunset RFC |
| RP-089 | D-005 previously required R-003 participant results before choosing recovery posture, while R-003 asks participants to use the absent workflow that only D-005 can authorize. | research/decision dependency cycle | reconciled: owner posture first, accepted prototype second, R-003 validation third |
| RP-090 | Active RFC dependency headers use stale whole-RFC statuses and omit required resource edges: UI lacks API/Account authority, Commons lacks Account/API/Game UI, World lacks telemetry/public consumers, Bots lacks Minigame integration, and Deployment lacks client/license/Account/API/operations contracts. | dependency graph and authority defect | ruling-author header/body reconciliation against `dependency-resource-ledger.tsv` before acceptance or archival |
| RP-091 | The execution handoff still named R-001 plus lifecycle reconciliation as the smallest safe batch after R-001 became authority-blocked and lifecycle reconciliation completed. | stale executable-queue handoff | reconciled to designated audit review then serial Q-001/Q-002/Q-003 manifest |
| RP-092 | The queue/review handoff was written as if the repository audit were ready for its final designated pass while Wave 1 still explicitly left package, route, migration, client, catalog, executable, planning, docs, and archived-RFC populations incomplete and Wave 2 remained coarse. | audit sequencing/scope defect | handoff downgraded to bounded draft; finish Waves 1–2 before final cross-party review |
| RP-093 | The counted inventory claimed 44 server package directories and four command entrypoints; exact reconciliation proves 45 directories and five entrypoints, with 189 production plus 151 test files. | inventory/current-status defect | corrected counts plus `server-package-inventory.tsv`; final contradiction pass must recompute every summary count |
| RP-094 | Exactly 41 of 82 client source files sit outside the shipped main/Worker/type dependency graph; parity engines, catalog parsers, fixtures, and helpers were easy to mistake for mounted product breadth despite having no production player consumer. | capability/inventory classification defect | `client-source-inventory.tsv` plus per-workflow producer→consumer verdicts; never promote module parity to player capability |
| RP-095 | The bundled Shell records reconciliation and Worker telemetry in memory, but `ShellController.telemetry()` has no production caller or exporter; only a unit test reads it. | orphaned instrumentation primitive | D-016/R-009 and one accepted gameplay-telemetry owner before operational claims |
| RP-096 | All four `copy/catalog/*.json` files are unconditionally merged into the shipped 208-key artifact, so three `*-candidate.json` files are release inputs despite their candidate labels; the repository has no machine-enforced held-vs-shipped source boundary. | content lifecycle/boundary defect | accepted copy-source manifest/status contract or rename/adopt candidates transactionally |
| RP-097 | The green copy pipeline reports 161 orphan keys but marks direct production Game UI calls such as `chrome.run_title.company_fallback`, `desk.buy_one`, and `screen.run_end.founder_note` orphaned because client `t()` sites are not enumerated. | acceptance-oracle/evidence defect | complete client+server+artifact reference extraction with seeded live/orphan negatives |
| RP-098 | The inventory counted 56 client test artifacts, but 13 are ignored local PNG captures with no screenshot assertion or fresh-clone availability; only 43 test sources are tracked. | evidence/shared-memory classification defect | split authoritative test sources from non-authoritative captures; never cite ignored PNG count as acceptance |
| RP-099 | `verify-routes-boundary` and `verify-commons-boundary` override the mandated repository-local Go cache with task-named `/tmp` caches inside the root Makefile. | tooling/process contradiction | use the root exported `GOCACHE`/approved package selectors and retain failing import fixtures |
| RP-100 | The raw 72-target Make count blends setup, ten tracked-output mutators, aliases, manual/historical measurements, partial checks, and five release-invalid aggregates; target existence was not an executable-evidence population. | evidence inventory/current-status defect | `make-target-inventory.tsv` classification plus per-gate objective/oracle evidence before release claims |
| RP-101 | Thirty-nine test sites across 27 server files silently skip when `TEST_DATABASE_URL` is absent, so an ordinary host `make test-go` green can exclude the Postgres integration population without exposing its denominator. | test-lane/evidence defect | require Docker/hosted Postgres lane and visible executed/skipped population for integration claims |
