route:
  receiver: operator
  group_by: [alertname]
  group_wait: 0s
  group_interval: 1m
  repeat_interval: 4h

receivers:
  - name: operator
    webhook_configs:
      - url: https://alerts.example.com/cloud-clicker
        send_resolved: true
