services:
  game-ui-postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: cloud_clicker_game_ui_test
      POSTGRES_USER: cloud_clicker
      POSTGRES_PASSWORD: cloud_clicker_game_ui_test
    ports:
      - "127.0.0.1:55433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cloud_clicker -d cloud_clicker_game_ui_test"]
      interval: 1s
      timeout: 5s
      retries: 30
    tmpfs:
      - /var/lib/postgresql/data
